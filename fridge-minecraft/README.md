# Fridge Minecraft

Fabric client + server mods for Stream Core integration.

- Client mod (port 3852): stats HTTP endpoint
- Server mod (port 3853): commands + Chat Dynamo / Kinetic redstone

See BUILD.md in each mod folder. Rebuild after pulling.

JARs are not included in this repo — place them in `jars/` or build yourself.
